{
  "name": "domedoaoplay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "esbuild css/style.css --minify --outfile=css/style.min.css && esbuild css/legal.css --minify --outfile=css/legal.min.css && esbuild js/main.js --minify --outfile=js/main.min.js"
  },
  "dependencies": {
    "compression": "^1.8.2",
    "express": "^4.21.2",
    "pdfkit": "^0.20.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
